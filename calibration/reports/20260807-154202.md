---
reviewed_at: 2026-08-07T15:42:02.769942+09:00
previous_report: null
session_cursor: 20260807_034036_f02fbdae
reviewed_count: 2
unreviewable_count: 2
backlog: false
status: candidate-review
---

# 야간 캘리브레이션 검토

## 범위
- 마지막 성공 보고서가 없어 2026-08-07의 최근 Slack 이미지 편집 세션을 처음부터 검색했다.
- Slack source 이미지 편집 세션 2건을 검토했다. 한 세션에는 request ID 2개가 있어 요청별로 분리했다.
- 첨부 인계 실패, renderer 오류, 300초 timeout, 후속 부정 품질 피드백 순으로 우선했다.
- CLI 포스트모텀은 후속 피드백과 비식별 지표의 상관관계에만 사용했다. 사용자 문장, 이미지 내용, 프롬프트, 세션 전문은 복사하지 않았다.
- 원본과 결과가 모두 존재한 요청 1건만 `vision_analyze`로 전후 비교했다. `unreviewable_count`는 요청 기준이다.

## 요청별 판정

### session `20260807_034036_f02fbdae` / request `img_97feca61d474`
- 상태: `reviewed`
- 도구/engine: Remaster `1.0.0`
- 파라미터: Brightness `+5`, Contrast `+3`, Highlights `-10`, Shadows `+12`, Saturation `+4`, Temperature `+5`, Tint `+1`, Sharpness `+7`, Denoise `+5`, Vignette `0`
- evidence:
  - `base-correction-before-creative-grade` `1.0.0`
  - `detail-preserving-denoise-and-edge-sharpen` `1.0.0`
  - `diagnostic-tonal-shaping-and-endpoints` `1.0.0`
  - `restrained-warm-cool-separation` `1.0.0`
- 실행: EditPlan 1.0 검증은 통과했다. MCP `inspect_image`와 `apply_remaster`는 입력 누락으로 실패했고, 로컬 workspace에서 동일 renderer·파라미터로 fallback 실행됐다.
- 비식별 지표: 평균 명도 `75.47 → 93.52`; 저명도 percentile `0.1%: 10 → 55`, `1%: 19 → 56`, `5%: 29 → 60`; 평균 절대 채널 차이 약 `18.19`. 해상도 유지, 명확한 black/white clipping 없음, 결과 PNG 메타데이터 없음.
- 시각 판정: 암부가 전역으로 크게 상승해 명암 깊이와 검정점이 약해졌다. 배경 저명도도 함께 상승해 피사체-배경 분리가 약해졌고 피부색은 의도한 온기보다 창백하고 회색에 가까워졌다. 정체성·구도·세부 구조는 보존됐으며 뚜렷한 halo, 링잉, 색 번짐, clipping은 보이지 않았다.
- 분류:
  - `capability 불일치`: 국소 톤·온냉 분리와 에지 제한 샤프닝을 전역 Remaster로 충분히 구현할 수 있다고 판단했다.
  - `parameter calibration`: `Brightness +5`와 `Shadows +12`의 누적 효과를 과소평가했다.
  - `renderer`: Remaster v1.0.0 shadow lift가 작은 숫자보다 강한 저명도 이동을 만들었다.
  - `workspace handoff`: Slack cache와 MCP jobs root 불일치로 표준 실행이 실패했다.
  - `검수 누락`: clipping·해상도·메타데이터는 확인했으나 저명도 percentile, 국소 대비, 상대 분리, 원본 대비 개선 여부를 실패 gate로 보지 않았다.
  - `불필요한 호출`: 입력 누락 원인이 해소되지 않은 상태에서 inspection 실패가 반복되고 fallback 경로 탐색이 길어졌다.
- 신뢰도: `0.98`

### session `20260807_034036_f02fbdae` / request `img_97feca61d474_genai`
- 상태: `unreviewable`
- 계획 도구/engine: Generate AI, Codex `$imagegen` built-in, adapter `2.1.0`; 확인된 Codex CLI `0.146.1`
- 파라미터 요약: operation `edit`, use case `identity-preserve`, output `png`, 보존 constraints 6개, 회피 조건 5개. 프롬프트 본문은 저장하지 않았다.
- evidence:
  - `natural-subject-background-separation` `1.0.0`
  - `semantic-mask-composition-and-edge-check` `1.0.0`
  - `natural-skin-tone-and-texture-preservation` `1.0.0`
- 실행: 최초 검증은 `use_case` 누락으로 실패한 뒤 보완되어 통과했다. Codex executable 미탐지, npm 설치 권한 실패, 기존 경로 충돌, 인증 파일 가독성 실패가 순차 발생했다. 권한 변경 후 재시도는 MCP 300초 timeout으로 끝났고 결과 파일이 생성되지 않았다.
- unreviewable 이유: 원본은 있었지만 결과가 없어 전후 비교 조건을 충족하지 못했다.
- 분류: `renderer`, `workspace handoff`, `불필요한 호출`, `검수 누락`.
- 신뢰도: `0.99`

### session `20260807_032640_38ed53cc` / request `7b1d9e5d-39ff-43d1-a509-ec6447018b2a`
- 상태: `unreviewable`
- 도구: `inspect_image`만 호출, renderer 미실행
- engine/calibration version: transcript에 기록 없음
- 파라미터/evidence: 없음
- 이유: Slack attachment 다운로드가 인증 또는 file permission 경계에서 실패해 원본과 결과가 모두 없었다.
- 분류:
  - `workspace handoff`: 첨부가 asset으로 전달되지 않아 request workspace 입력이 없었다.
  - `이미지 해석`: 입력이 없어 수행 불가했으며 추측하지 않고 중단한 것은 적절했다.
  - `불필요한 호출`: 실패 공지 후 inspection 1회가 추가됐으나 즉시 중단해 재시도 폭주는 없었다.
- 신뢰도: `1.00`

## 반복 패턴
- `workspace handoff` 문제가 독립 Slack 세션 2건에서 반복됐다. 한 건은 attachment 다운로드, 다른 한 건은 cache와 MCP jobs root 사이에서 실패했다.
- Generate AI 실행 환경 preflight 부재는 request 1건에서 확인됐다. executable·인증 가독성·timeout budget이 렌더 전에 확정되지 않았다.
- Remaster 과도한 저명도 상승은 독립 세션 1건과 후속 부정 품질 피드백으로만 확인됐다. 한 사례를 production 범위로 일반화하지 않는다.
- evidence의 도구 단위 `supported_tools`와 실제 세부 capability 사이에 간극이 있다. 전역 UnsharpMask는 에지 제한 샤프닝이 아니고 전역 색 gain은 피사체·배경 온냉 분리가 아니다.

## Calibration 후보

### A. Remaster v1.0.0 저명도 누적 반응
- 후보 출발 범위: Brightness `+1~+2`, Shadows `+3~+5`, Highlights `-3~-5`, Contrast `+1~+2`; 진단 근거가 없으면 Saturation·Tint·Denoise·Sharpness는 `0` 또는 최소값에서 시작한다.
- 필요한 benchmark: 서로 다른 저노출/로우키 fixture, Brightness·Shadows 단독 및 조합 비교, 저명도 percentile·국소 대비·상대 분리·피부색 blind A/B, golden/contract 무회귀.
- 위험: 실제 저노출 복구가 필요한 이미지에 지나치게 보수적일 수 있다.
- 자동 반영 금지: `true`. 여러 독립 세션과 benchmark 무회귀가 필요하다.

### B. evidence-to-capability gate 세분화
- `global-tone`, `local-mask`, `edge-limited-sharpen`, `subject-background-color-separation` 등 실제 capability 충족 여부를 계획 단계에서 판정한다.
- benchmark: 동일 의도를 Remaster와 masking 가능한 경로에서 비교하고 부분 지원 warning의 과도한 도구 전환 여부를 확인한다.
- 위험: 엄격한 분류가 안전한 전역 미세 보정까지 차단할 수 있다.
- 자동 반영 금지: `true`.

### C. Generate AI 실행 preflight
- 실제 호출 전에 Codex executable/version, MCP 사용자 기준 config/auth 가독성, input workspace, output 경로, timeout budget을 한 번에 확인한다.
- benchmark: 정상, executable 부재, 인증 권한 실패, timeout, 결과 미생성 fixture에서 빠른 실패와 오류 분류를 검증한다.
- 위험: 자격증명 내용은 검사 결과나 로그에 노출하면 안 된다.
- 자동 반영 금지: `true`.

## 불필요한 과정 후보
- 입력 누락이 확인되면 같은 workspace root를 대상으로 inspection 또는 renderer를 반복하지 않고 handoff 상태를 한 번 진단한다.
- Generate AI는 schema validation과 executable·credential·workspace preflight를 모두 통과한 뒤 장시간 렌더를 시작한다.
- 시스템 전역 npm 설치, 기존 binary가 있는 상태의 중복 설치, 권한 없는 `chown`·`sudo` 시도는 생략한다.
- nightly Remaster 검토에서는 clipping·해상도·메타데이터만 보지 않고 저명도 percentile과 side-by-side 비교를 함께 사용한다.
- production 자동 수정은 금지하며 반복 증거와 회귀 테스트 후 별도 승인한다.

## 다음 cursor와 backlog
- 다음 cursor: `20260807_034036_f02fbdae`
- 검토 Slack 세션: 2건
- 요청별 판정: 품질 비교 완료 1건, unreviewable 2건
- 남은 대상 추정: 0건
- backlog: `false`
