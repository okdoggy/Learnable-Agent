---
reviewed_at: 2026-08-08T21:00:30.161776+09:00
previous_report: calibration/reports/20260807-210020.md
session_cursor: 20260807_094447_dc0e3189
reviewed_count: 5
unreviewable_count: 5
backlog: true
status: candidate-review
---

# 야간 캘리브레이션 검토

## 범위
- 이전 보고서 이후 새 Slack 이미지 편집 세션은 확인되지 않았다. 이전 보고서가 남긴 backlog에서 미검토 세션 5건을 최근 순으로 검토했다.
- 성공 4건은 결과 파일만 TTL 안에 남아 있고 원본 첨부가 만료되어 전후 비교 조건을 충족하지 못했다. 실패 1건은 결과와 request ID가 생성되지 않았다. 누락 파일에 대한 반복 호출은 하지 않았다.
- 사용자 부정 피드백은 확인되지 않았다. 사용자 문장, 이미지 내용, 실행 프롬프트, 세션 전문, 토큰, 파일 경로 및 비공개 URL은 저장하지 않았다.

## 요청별 판정

### `20260807_092250_999dfafb` / `req_slack_167cc7a1e9b5`
- 도구·버전: Remaster 단일 단계, EditPlan `1.0`; renderer 및 calibration version은 transcript 반환값에 노출되지 않았다.
- 실행 파라미터: `brightness 0`, `contrast +1`, `highlights -3`, `shadows +5`, `saturation +2`, `temperature 0`, `tint 0`, `sharpness +1`, `denoise 0`, `vignette 0`.
- evidence: `base-correction-before-creative-grade` v1.0.0, `diagnostic-tonal-shaping-and-endpoints` v1.0.0, `detail-preserving-denoise-and-edge-sharpen` v1.0.0.
- 분류: `검수 누락`, `unreviewable`.
- 관찰: 계획은 전역 밝기 상승을 피하고 하이라이트 보호와 제한적 암부 회복을 조합했다. 결과 파일은 존재하지만 원본이 TTL에서 만료되어 실제 하이라이트 보존, 암부 가독성 및 구조 보존을 전후 비교할 수 없다.
- unreviewable 이유: 원본 파일 부재.
- 신뢰도: 실행 사실과 파일 상태는 높음, 품질 판정은 불가.

### `20260807_091006_53eb5153` / `req_slack_43c776931cdd`
- 도구·버전: Remaster 단일 단계, EditPlan `1.0`; renderer 및 calibration version은 transcript 반환값에 노출되지 않았다.
- 실행 파라미터: `brightness +7`, `contrast +2`, `highlights -3`, `shadows +9`, `saturation +1`, `temperature +2`, `tint +2`, `sharpness +1`, `denoise +2`, `vignette 0`.
- evidence: `base-correction-before-creative-grade` v1.0.0, `diagnostic-tonal-shaping-and-endpoints` v1.0.0, `detail-preserving-denoise-and-edge-sharpen` v1.0.0.
- 분류: `parameter calibration` 후보, `검수 누락`, `unreviewable`.
- 관찰: 입력 진단상 강한 저노출과 검정 클리핑을 근거로 했지만 `brightness +7`과 `shadows +9`의 누적 상승은 보수적 시작 범위를 크게 벗어난다. 결과 파일은 존재하나 원본이 만료되어 검정점 약화, 중간톤 평탄화, 노이즈 상승 또는 실제 복원 이득을 확인할 수 없다.
- unreviewable 이유: 원본 파일 부재.
- 신뢰도: 범위 이탈 관찰은 높음, 시각적 결함 여부는 판정 불가.

### `20260807_084959_1844a0f1` / `req_slack_6c5d1e6ca81a`
- 도구·engine: Generate AI, `openai-image-api`, `gpt-image-2`, quality `low`, use case `identity-preserve`, `1024x1024` PNG, EditPlan `1.0`; calibration version은 transcript 반환값에 노출되지 않았다.
- 파라미터: 생성 편집 단일 단계. 정체성·구도·의상·배경 구조·피부 질감 보존 제약과 재구성 회피 조건이 포함됐다.
- evidence: `natural-skin-tone-and-texture-preservation` v1.0.0, `natural-subject-background-separation` v1.0.0, `semantic-mask-composition-and-edge-check` v1.0.0, `restrained-warm-cool-separation` v1.0.0.
- 분류: `capability 불일치` 후보, `renderer`, `검수 누락`, `unreviewable`.
- 관찰: 입력 규격은 가로형이었으나 당시 renderer는 정사각형 고정 출력을 사용했다. 계획 자체가 크롭·구도 변화와 사후 비교 gate 부재를 경고했음에도 실행과 전달이 계속됐다. 결과는 존재하지만 원본이 만료되어 정체성, 프레이밍, 배경 구조 및 경계 보존을 전후 비교할 수 없다.
- unreviewable 이유: 원본 파일 부재.
- 신뢰도: 규격 불일치는 높음, 실제 보존 실패 여부는 판정 불가.

### `20260807_084757_b36b8071` / `req_slack_b35db5ea1680`
- 도구·버전: Remaster 단일 단계, EditPlan `1.0`; renderer 및 calibration version은 transcript 반환값에 노출되지 않았다.
- 실행 파라미터: `brightness +4`, `contrast +1`, `highlights -2`, `shadows +4`, `saturation +1`, `temperature +1`, `tint +1`, `sharpness +1`, `denoise +1`, `vignette 0`.
- evidence: `base-correction-before-creative-grade` v1.0.0, `diagnostic-tonal-shaping-and-endpoints` v1.0.0, `detail-preserving-denoise-and-edge-sharpen` v1.0.0.
- 분류: `검수 누락`, `unreviewable`.
- 관찰: 생성형 편집의 구조 재구성 위험을 피하고 저강도 전역 교정을 선택한 것은 capability에 부합한다. 다만 결과만 남아 있어 피부색, 하이라이트, 암부 노이즈와 실제 구조 보존을 검증할 수 없다.
- unreviewable 이유: 원본 파일 부재.
- 신뢰도: 계획 적합성은 중간, 품질 판정은 불가.

### `20260807_082943_b163aaca` / request ID 없음
- 도구·버전·파라미터·evidence: planner와 request ID 생성 전에 `413 Content Too Large`가 발생해 확인 불가.
- 분류: `renderer`, `workspace handoff`, `unreviewable`.
- 관찰: 이미지 처리 fast path에서 upstream request payload 제한에 걸렸으며 결과가 생성되지 않았다. 동일 세션 내 재시도는 없었다.
- unreviewable 이유: 결과 파일과 request ID 부재.
- 신뢰도: 높음.

## 반복 패턴
- 독립 세션 2건에서 planner와 request ID 생성 전 `413 Content Too Large`가 확인됐다. 이번 보고서가 상세 검토한 1건 외 동일 시기 미검토 backlog 1건에서도 같은 실패가 검색되어 `renderer/workspace handoff` 반복 후보로 볼 수 있다. 다만 payload 구성과 gateway 제한 로그를 직접 조회하지 않았으므로 근본 원인은 확정하지 않는다.
- Generate AI의 종횡비·보존 위험은 이전 보고서의 독립 `identity-preserve` 사례와 이번 사례에서 반복적으로 드러났다. 이번 사례는 원본 만료로 실제 품질 결함을 재확인하지 못했으므로 규격 불일치와 gate 부재만 반복 근거로 사용한다.
- Remaster의 `brightness`와 `shadows` 동시 범위 초과는 이전 보고서 사례에 이어 별도 세션에서 다시 확인됐다. 이번 값은 더 높지만 원본 만료로 결과 결함은 확인하지 못했다.

## Calibration 후보
- Remaster 누적 톤 후보: 양의 `brightness`와 `shadows`가 동시에 보수 범위를 넘으면 검정점, 중간톤 평탄화, 노이즈와 하이라이트 여유를 검사하는 사후 benchmark를 요구하는 방안을 검토한다.
  - 필요한 benchmark: 저노출·검정 클리핑 수준별 독립 샘플, 히스토그램·검정점·국부 대비·노이즈·주관 품질 비교.
  - 예상 위험: 실제로 큰 암부 회복이 필요한 장면을 과도하게 제한할 수 있다.
  - production 자동 반영 금지: 예. 여러 독립 세션의 실제 전후 결함, 사용자 피드백, benchmark 무회귀가 필요하다.
- Generate AI 종횡비·보존 후보: 지원 출력 규격과 입력 종횡비 차이가 큰 `identity-preserve` 실행은 전달 전 구조·프레이밍 보존 gate를 요구하는 방안을 검토한다.
  - 필요한 benchmark: 다양한 종횡비의 인물 샘플, 얼굴 유사도, 랜드마크·배경 대응, 크롭 비율, 경계 artifact와 사용자 평가.
  - 예상 위험: 추가 지연·비용과 보존 gate 오탐.
  - production 자동 반영 금지: 예. 현재는 한 사례가 시각적으로 unreviewable이며 이전 독립 사례와의 반복만 확인됐다.
- 입력 handoff 용량 후보: planner 호출 전에 이미지가 포함된 payload 크기와 gateway 상한을 deterministic하게 검사하고, 지원 가능한 정규화 경로가 있을 때만 1회 변환하는 benchmark를 검토한다.
  - 필요한 benchmark: 해상도·압축률·파일 크기 조합별 payload 크기, latency, 품질 손실, request ID 발급 및 멱등성.
  - 예상 위험: 자동 축소의 품질 저하와 사용자 원본 변형.
  - production 자동 반영 금지: 예. 원인 계층 확인과 benchmark 무회귀가 선행되어야 한다.

## 불필요한 과정 후보
- 성공 세션마다 coordinator skill을 로드한 뒤 fast path를 한 번만 호출한 흐름은 현재 정책 계약에 부합하며 제거 후보로 확정하지 않는다.
- 결과만 남고 원본이 만료된 사례에서 결과 단독 `vision_analyze`는 전후 판정에 사용할 수 없다. nightly reviewer는 양쪽 파일 존재를 먼저 확인하고 한쪽이 없으면 즉시 `unreviewable`로 종료해야 한다.
- `413` 실패 뒤 반복 렌더링이나 경로 탐색을 하지 않은 것은 적절했다. 향후 자동 변환 또는 재시도는 payload 상한, 품질 손실, request ID 발급 전 단계와 멱등성이 검증된 경우에만 1회로 제한해야 한다.

## 다음 cursor와 backlog
- 다음 cursor: `20260807_094447_dc0e3189` 유지. 이전 보고서 이후 새 Slack 이미지 편집 세션이 없었으므로 최신 시간 cursor를 뒤로 이동하지 않았다.
- 이번 검토: 5건, 모두 `unreviewable`.
- 검색 결과에서 이번 한도 때문에 미검토 Slack 이미지 편집 세션이 최소 1건 더 확인됐다. 검색 상한 밖 추가 세션 수는 확정하지 않았다.
- backlog: `true`.
