---
reviewed_at: 2026-08-07T21:00:20.742713+09:00
previous_report: calibration/reports/20260807-155123.md
session_cursor: 20260807_094447_dc0e3189
reviewed_count: 5
unreviewable_count: 3
backlog: true
status: candidate-review
---

# 야간 캘리브레이션 검토

## 범위
- 이전 보고서 cursor `20260807_034036_f02fbdae` 이후 검색된 Slack 이미지 편집 세션 가운데 오류·장시간 연결 종료를 우선하고, 최근 성공 결과 2건을 함께 대조했다.
- 최대 검토 한도 5건에 따라 연결 종료 3건과 Remaster·Generate AI 성공 1건씩을 선택했다.
- 성공 2건은 원본과 결과가 모두 TTL 안에 존재해 `vision_analyze`로 개발용 전후 비교를 수행했다. 실패 3건은 결과 파일과 request ID가 생성되지 않아 반복적인 경로 탐색이나 이미지 비교를 하지 않았다.
- 사용자 문장, 이미지 내용, 실행 프롬프트, 세션 전문, 토큰, 파일 경로 및 비공개 URL은 보고서에 저장하지 않았다.

## 요청별 판정

### `20260807_094447_dc0e3189` / `req_slack_c5f9dca227f3`
- 도구·버전: Remaster engine `1.0.0`, calibration `1.0.0`, EditPlan `1.0`.
- 실행 파라미터: 1단계 `denoise +1`; 2단계 `brightness +6`, `contrast +1`, `shadows +6`, `sharpness +2`; 나머지 `0`.
- evidence: `detail-preserving-denoise-and-edge-sharpen` v1.0.0, `base-correction-before-creative-grade` v1.0.0, `diagnostic-tonal-shaping-and-endpoints` v1.0.0.
- 분류: `parameter calibration` 관찰 후보, `검수 누락`.
- 관찰: 원본·결과가 모두 존재했다. 전후 비교에서 암부 가독성은 완만하게 개선됐고 눈에 띄는 halo·색 불안정·노이즈 증폭은 확인되지 않았다. 다만 `brightness +6`과 `shadows +6`은 각각 보수적 시작 범위를 벗어나며 누적 상승 조합이다. 이번 결과는 허용 가능했지만 자동 전후 gate가 없어 동일 조합의 검정점 약화 위험을 실행 시점에 검증하지 못했다.
- 신뢰도: 중간. 단일 장면에서 품질 결함이 재현된 것은 아니며 production 조정 근거로 일반화할 수 없다.

### `20260807_093715_742dab7f` / `req_slack_980dcb902749`
- 도구·engine: Generate AI, `openai-image-api`, `gpt-image-2`, quality `low`, use case `identity-preserve`, 출력 `1536x1024` PNG; calibration `1.0.0`, EditPlan `1.0`.
- 파라미터: 생성 편집 단일 단계. 정체성·구도·기존 텍스트·배경 구조 보존 제약과 재구성 회피 조건이 포함됐다.
- evidence: `natural-skin-tone-and-texture-preservation` v1.0.0, `natural-subject-background-separation` v1.0.0, `semantic-mask-composition-and-edge-check` v1.0.0.
- 분류: `capability 불일치`, `renderer`, `검수 누락`.
- 관찰: 원본·결과가 모두 존재했다. 얼굴 음영과 전체 가독성은 개선됐으나, 전후 비교에서 인물 세부 정체성, 머리카락·의상 형상, 배경 세부와 주변 요소가 유의미하게 재구성됐다. 입력 4:3에 대해 지원 규격상 가장 가까운 가로형 3:2 출력이 선택되어 프레이밍도 완전히 동일하지 않았다. 일부 기존 텍스트는 유지됐지만, 강한 보존 제약만으로 `low` 품질 생성 편집의 픽셀·구조 보존을 보장하지 못했다.
- 신뢰도: 높음. 보존 제약과 실제 결과의 차이가 전후 비교에서 직접 확인됐다. 단, 한 사례만으로 engine 또는 프롬프트를 production에서 자동 변경해서는 안 된다.

### `20260807_092831_5718fb6a` / request ID 없음
- 도구·버전·파라미터·evidence: 계획 생성 전에 연결이 종료되어 확인 불가.
- 분류: `renderer`, `workspace handoff`, `unreviewable`.
- 관찰: `ClosedResourceError`로 결과와 request ID가 생성되지 않았다. 같은 세션 안에서 불필요한 재시도는 없었다.
- unreviewable 이유: 결과 파일이 없어 전후 품질 비교가 불가능하다.
- 신뢰도: 높음.

### `20260807_092444_d77374d6` / request ID 없음
- 도구·버전·파라미터·evidence: 계획 생성 전에 MCP 연결이 종료되어 확인 불가.
- 분류: `renderer`, `workspace handoff`, `unreviewable`.
- 관찰: 비교적 긴 대기 뒤 연결 종료가 노출됐고 결과가 생성되지 않았다. 실패 후 반복 호출은 없었다.
- unreviewable 이유: 결과 파일이 없고 request ID도 발급되지 않았다.
- 신뢰도: 높음.

### `20260807_084431_9536a1a2` / request ID 없음
- 도구·버전·파라미터·evidence: 계획 생성 전에 편집 서버 연결이 종료되어 확인 불가.
- 분류: `renderer`, `workspace handoff`, `unreviewable`.
- 관찰: 결과와 request ID가 생성되지 않았다. 실패 후 빈 후속 메시지에 일반 응답이 한 차례 있었으나 편집 재시도는 수행하지 않았다.
- unreviewable 이유: 결과 파일이 없어 전후 품질 비교가 불가능하다.
- 신뢰도: 높음.

## 반복 패턴
- 독립 Slack 세션 3건에서 계획·request ID 생성 전 연결 종료가 반복됐다. 동일한 `renderer/workspace handoff` 계열 후보로 묶을 수 있으나, 이 보고서는 원인 로그를 직접 조회하지 않았으므로 timeout, gateway 또는 MCP 수명주기 중 어느 층이 근본 원인인지는 확정하지 않는다.
- Generate AI 보존 실패는 이번 검토에서 1건만 확인됐다. 사용자 부정 피드백은 확인되지 않았으므로 단일 취향이나 단일 결과를 일반화하지 않는다.
- Remaster의 `brightness`와 `shadows` 동시 범위 초과는 1건이며, 이번 결과에서 명백한 과보정은 확인되지 않았다.

## Calibration 후보
- Generate AI `identity-preserve` 후보: 실행 후 정체성·구도·기존 텍스트·배경 구조 보존을 자동 또는 오프라인 benchmark로 점검하고, 임계 실패 시 결과를 바로 전달하지 않는 품질 gate를 검토한다.
  - 필요한 benchmark: 서로 다른 조명·종횡비·텍스트 포함 인물의 독립 샘플, 얼굴 유사도와 구조 변화, OCR 보존, 배경 대응, 크롭 변화, 사용자 평가.
  - 예상 위험: 지나치게 엄격한 gate의 오탐과 추가 지연·비용.
  - production 자동 반영 금지: 예. 여러 독립 세션의 반복, 사용자 피드백, benchmark 무회귀가 필요하다.
- Remaster 누적 톤 후보: `brightness`와 양의 `shadows`가 동시에 보수 범위를 넘을 때 검정점·중간톤 평탄화 검사를 요구하는 benchmark를 검토한다.
  - 필요한 benchmark: 저노출·정상 노출·강한 명암 장면별 히스토그램, 검정점, 국부 대비, 주관 품질 비교.
  - production 자동 반영 금지: 예. 이번 단일 결과는 허용 가능했으므로 파라미터 상한 변경 근거가 아니다.

## 불필요한 과정 후보
- 세 실패 세션 모두 결과가 없을 때 추가 이미지 경로 탐색이나 `vision_analyze`를 수행하지 않은 조기 종료는 적절했다.
- Slack fast path에서 매 요청마다 coordinator skill을 별도 로드하는 호출은 반복되지만, 현재 정책·응답 계약을 보장하는 전제이므로 근거 없이 제거하지 않는다. 향후 runtime에 동일 계약이 고정되고 회귀 테스트가 있을 때만 호출 축소를 검토한다.
- 연결 종료 뒤 동일 세션에서 자동 무한 재시도는 없었다. 재시도를 추가한다면 request ID 발급 여부와 멱등성이 확인된 경우에만 1회로 제한해야 한다.

## 다음 cursor와 backlog
- 다음 cursor: `20260807_094447_dc0e3189`.
- 이번 검토: 5건, 그중 `unreviewable` 3건.
- 검색 결과에서 이번 한도 때문에 미검토 Slack 이미지 편집 세션이 최소 4건 더 확인됐다. 검색 상한 밖 추가 세션 수는 확정하지 않았다.
- backlog: `true`.
